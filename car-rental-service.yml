server:
  port: 7777

spring:
  application:
    name: car-rental-service

  datasource:
    url: jdbc:mysql://localhost:3306/car_db
    username: arpit
    password: arp01

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
